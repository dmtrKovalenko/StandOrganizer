export default class Servce {
  constructor(id, startTime, endTime, whitness, assistant) {
    this.id = id;
    this.startTime = startTime;
    this.endTime = endTime;
    this.whitness = whitness;
    this.assistant = assistant;
  }
}
